// postcss.config.js
// Tailwind CSS v4 requires @tailwindcss/postcss as the PostCSS plugin.
// Using CommonJS (.js) format — reliably picked up by Next.js on Vercel.
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
